version: '2'

services:
  mysql_test:
        container_name: pedidos_database
        image: mysql
        ports:
                - "3306:3306"
        environment:
           MYSQL_ROOT_PASSWORD: 1234
           MYSQL_USER: root
        volumes:
           - /ArchivosEnviados/:/docker-entrypoint-initdb.d
