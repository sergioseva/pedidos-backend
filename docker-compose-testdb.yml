version: '2'

services:
  mysql_test:
        container_name: pedidos_database_test
        image: mysql
        ports:
                - "3306:3306"
        environment:
           MYSQL_ROOT_PASSWORD: 1234
           MYSQL_USER: pedidos
        volumes:
           - /tmp/:/docker-entrypoint-initdb.d
